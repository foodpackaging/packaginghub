<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
        Android 9+ blocks cleartext HTTP by default, which breaks talking to a local
        backend over http://. Production traffic stays HTTPS-only; only the local
        development hosts below are allowed to use cleartext.

        Testing on a physical device? Add your machine's LAN IP as another <domain>
        entry, e.g. <domain includeSubdomains="false">192.168.1.5</domain>
    -->
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <!-- The host machine's localhost, as seen from inside the Android emulator. -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
